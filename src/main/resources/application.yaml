spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/WMS
    username: wmsUser
    password: wmsUser123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true